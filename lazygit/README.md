# lazygit

Auf einem neuen Rechner einmalig verlinken:

```sh
# macOS
ln -sf ~/.config/nvim/lazygit/config.yml ~/Library/Application\ Support/lazygit/config.yml

# Linux
ln -sf ~/.config/nvim/lazygit/config.yml ~/.config/lazygit/config.yml
```

Braucht `delta` (`brew install git-delta`).
